{% extends 'base.html.twig' %}

{% block title %}Edit Info{% endblock %}

{% block body %}
    <h1 class="text-center text-primary display-4">Editer Ressource</h1>

    {{ include('info/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" href="{{ path('info_index') }}">retour à la liste</a>

    <a class="btn btn-primary">{{ include('info/_delete_form.html.twig') }}</a>
{% endblock %}
