{% extends 'base.html.twig' %}

{% block title %}Keyword{% endblock %}

{% block body %}
    <h1>Keyword</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ keyword.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ keyword.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('keyword_index') }}">back to list</a>

    <a href="{{ path('keyword_edit', {'id': keyword.id}) }}"><i class="fas fa-edit"></i></a>

    {{ include('keyword/_delete_form.html.twig') }}
{% endblock %}
